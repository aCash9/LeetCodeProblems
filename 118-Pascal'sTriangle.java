class Solution {
    public List<Integer> row(List<Integer> l)
    public List<List<Integer>> generate(int numRows) {
        List<List<Integer>> list = new LinkedList<>();
        for(int i = 0; i < numRows; i++){
            List<Integer> l = new LinkedList<>();
            
        }
    }
}
